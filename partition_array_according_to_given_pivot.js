//  CODE IN PYTHON

// class Solution:
//     def pivotArray(self, nums: List[int], pivot: int) -> List[int]:
//         ls = []
//         for num in nums:
//             if num < pivot:
//                 ls.append(num)
//         for num in nums:
//             if num == pivot:
//                 ls.append(num)
//         for num in nums:
//             if num > pivot:
//                 ls.append(num)
                
//         return ls
        